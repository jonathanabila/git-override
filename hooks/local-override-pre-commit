#!/usr/bin/env bash
#
# local-override-pre-commit
#
# Restores original content for overridden files before commit.
# This ensures commits contain the original tracked content, not local overrides.
#
# This hook reads recursive .local-overrides.yaml files and restores
# originals for any staged files that have local overrides.
#
# IMPORTANT: If ANY target in a group is staged, ALL targets in that group
# are restored. This ensures consistency for multi-target overrides.
#
# Performance target: <100ms for typical commits
#
set -euo pipefail

# Find the directory where this script lives
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Source the shared library
# shellcheck source=local-override-lib.sh
source "$SCRIPT_DIR/local-override-lib.sh"

main() {
    local repo_root
    local resolution_root
    local staged_files=""
    local relevant_configs=""
    local relevant_entries=""
    local overrides_to_restore=""
    local state_file=""
    local state_entries=""
    repo_root="$(get_repo_root)" || return 0

    # Configs and override files resolve against the resolution root (the main
    # checkout for fallback worktrees); index and working-tree operations stay
    # on the checkout root. No config-discovery cache here: everything below
    # resolves configs by direct path walks, never a global scan.
    resolution_root="$(get_resolution_root "$repo_root")"

    clear_post_commit_state "$repo_root"

    # Get list of staged files
    staged_files=$(git -C "$repo_root" diff --cached --name-only 2>/dev/null) || return 0

    # Quick exit if nothing staged
    [[ -z "$staged_files" ]] && return 0

    local staged_file=""
    local config_path=""
    local matched_config="false"
    while IFS= read -r staged_file || [[ -n "$staged_file" ]]; do
        [[ -n "$staged_file" ]] || continue
        config_path="$(find_nearest_config_for_path "$resolution_root" "$staged_file" 2>/dev/null || true)"
        [[ -n "$config_path" ]] || continue

        if ! echo "$relevant_configs" | grep -qxF "$config_path"; then
            relevant_configs="$relevant_configs
$config_path"
        fi
    done <<< "$staged_files"

    [[ -n "$relevant_configs" ]] || return 0

    local config_entry
    while IFS= read -r config_path || [[ -n "$config_path" ]]; do
        [[ -n "$config_path" ]] || continue

        while IFS= read -r config_entry || [[ -n "$config_entry" ]]; do
            [[ -n "$config_entry" ]] || continue
            relevant_entries="$relevant_entries
$config_entry"
        done < <(read_config_entries_for_file "$resolution_root" "$config_path")
    done <<< "$relevant_configs"

    [[ -n "$relevant_entries" ]] || return 0

    local repaired_count
    repaired_count="$(clear_legacy_skip_worktree "$repo_root" "$relevant_entries")"
    if [[ "$repaired_count" -gt 0 ]]; then
        printf 'git-local-override: cleared legacy skip-worktree on %s managed file(s)\n' "$repaired_count" >&2
    fi

    # First pass: find which override files have staged targets
    # We need to restore ALL targets for each such override file
    local entry override

    while IFS= read -r entry || [[ -n "$entry" ]]; do
        [[ -z "$entry" ]] && continue

        target="${entry%%|*}"
        override="${entry#*|}"

        # Check if this target is staged
        if echo "$staged_files" | grep -qxF "$target"; then
            # Check if override file exists
            if [[ -f "$resolution_root/$override" ]]; then
                # Add to list if not already present
                if ! printf '%s\n' "$overrides_to_restore" | grep -qxF "$override"; then
                    overrides_to_restore="$overrides_to_restore
$override"
                fi
            fi
        fi
    done <<< "$relevant_entries"

    # Quick exit if no overrides need restoring
    [[ -z "$overrides_to_restore" ]] && return 0

    state_file="$(get_post_commit_state_file "$repo_root")"
    : > "$state_file"

    # Second pass: for each override file to restore, restore ALL its targets
    local override_file
    while IFS= read -r override_file || [[ -n "$override_file" ]]; do
        [[ -z "$override_file" ]] && continue

        # Get all targets for this override file
        local target_file
        while IFS= read -r target_file || [[ -n "$target_file" ]]; do
            [[ -z "$target_file" ]] && continue

            # Check if file exists in HEAD (not a new file)
            if git -C "$repo_root" cat-file -e "HEAD:$target_file" 2>/dev/null; then
                if ! path_is_symlink_safe "$repo_root" "$target_file"; then
                    printf 'git-local-override: refusing symlinked path for %s\n' "$target_file" >&2
                    continue
                fi
                # Restore original from git
                git -C "$repo_root" show "HEAD:$target_file" > "$repo_root/$target_file"

                # Re-stage the restored file
                GIT_LOCAL_OVERRIDE_DISABLE=1 git -C "$repo_root" add "$target_file"
            fi
            # If file doesn't exist in HEAD, it's a new file - let it be committed as-is
        done < <(while IFS= read -r entry || [[ -n "$entry" ]]; do
            [[ -z "$entry" ]] && continue
            target="${entry%%|*}"
            override="${entry#*|}"
            if [[ "$override" == "$override_file" ]]; then
                printf '%s\n' "$target"
            fi
        done <<< "$relevant_entries")

        # Overrides are recorded as absolute paths so post-commit re-applies
        # from the resolution root, not the (possibly fallback) checkout.
        local state_entry
        while IFS= read -r entry || [[ -n "$entry" ]]; do
            [[ -z "$entry" ]] && continue
            target="${entry%%|*}"
            override="${entry#*|}"
            if [[ "$override" == "$override_file" ]]; then
                state_entry="$target|$resolution_root/$override"
                if ! printf '%s\n' "$state_entries" | grep -qxF "$state_entry"; then
                    state_entries="$state_entries
$state_entry"
                fi
            fi
        done <<< "$relevant_entries"
    done <<< "$overrides_to_restore"

    printf '%s\n' "$state_entries" > "$state_file"

    # The post-commit hook will re-apply overrides after commit completes

    return 0
}

main "$@"
